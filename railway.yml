build:
  builder: nixpacks
  buildCommand: npm run build && php artisan config:cache && php artisan route:cache && php artisan view:cache

deploy:
  startCommand: php artisan serve --host=0.0.0.0 --port=$PORT
  healthcheckPath: /
  healthcheckTimeout: 300
  restartPolicyType: always
